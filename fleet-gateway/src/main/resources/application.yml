server:
  port: 8080

spring:
  application:
    name: fleet-gateway
  threads:
    virtual:
      enabled: true
  cloud:
    gateway:
      mvc:
        routes:
          - id: quote_service
            uri: http://localhost:8081
            predicates:
              - Path=/api/quotes/**